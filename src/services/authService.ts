import { BaseService } from './baseService';
 
export class AuthService extends BaseService {
 
    // async login(credentials: { email: string; password: string }) {
    //     const response = await this.post(`${this.BASE_PATH}/login`, credentials);
    //     return this.handleResponse(response);
    // }
 
    // async register(userData: { email: string; password: string; firstName?: string; lastName?: string }) {
    //     const response = await this.post(`${this.BASE_PATH}/register`, userData);
    //     return this.handleResponse(response);
    // }
}
